<Window x:Class="DesktopTranslator.Views.RegionSelector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowState="Normal"
        Cursor="Cross"
        KeyDown="Window_KeyDown">

    <Grid>
        <!-- Dark overlay with a "hole" for the selected region -->
        <Canvas x:Name="OverlayCanvas" Background="#66000000"
                MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                MouseMove="Canvas_MouseMove"
                MouseLeftButtonUp="Canvas_MouseLeftButtonUp">

            <!-- Selection rectangle -->
            <Border x:Name="SelectionBorder"
                    BorderBrush="#0078D4"
                    BorderThickness="2"
                    Background="Transparent"
                    Visibility="Collapsed"
                    IsHitTestVisible="False">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="6" ShadowDepth="0" Opacity="0.4" Color="#0078D4" />
                </Border.Effect>
            </Border>

            <!-- Size indicator -->
            <Border x:Name="SizeIndicator"
                    Background="#CC0078D4"
                    CornerRadius="4"
                    Padding="8,4"
                    Visibility="Collapsed"
                    IsHitTestVisible="False">
                <TextBlock x:Name="SizeText"
                           Foreground="White"
                           FontSize="12"
                           FontFamily="Segoe UI Variable, Segoe UI"
                           FontWeight="SemiBold" />
            </Border>
        </Canvas>

        <!-- Instruction text at top -->
        <Border HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Margin="0,40,0,0"
                Background="#CC222222"
                CornerRadius="8"
                Padding="20,12"
                IsHitTestVisible="False">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="&#xE8B0;" FontFamily="Segoe MDL2 Assets" Foreground="White" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0" />
                <TextBlock Text="拖拽鼠标选择翻译区域    |    ESC 取消"
                           Foreground="White"
                           FontSize="14"
                           FontFamily="Segoe UI Variable, Segoe UI, Microsoft YaHei UI"
                           VerticalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
